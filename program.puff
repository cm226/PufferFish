fn t(f) {
    var TwoPI = 3.14 * 2.0;
    var frames = 500.0;
    var deltaRad = TwoPI;
    deltaRad = TwoPI / frames;

    var rad = f * deltaRad;

    x = sin(rad);
    y = cos(rad);

    x = x * 100.0;
    y = y * 100.0;
};

anim(t);